node {
    def app

    environment {
                ENV_HOST = "vhost90-public.wvnet.edu"
            }

    stage('Clone repository') {
        sh'''
        echo "my var is {ENV_HOST}"
        '''
    }
}